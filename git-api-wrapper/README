Steps :

1. run npm install 
2. CONFIG -> git-apis.js -> 
		example 

	Go to GITHUB -> Create application and generate token manually or You can also use authorization API to generate token 

		var apis   =  { 
			....

			'header' : {
				'Authorization' : 'token YourTOKEN’, //PLEASE PROVIDE YOUR TOKEN
				'Accept': 'application/json', 
				'Content-Type': 'application/json',
				'user-agent': 'node.js'
			}

			.....
		}

3. run node server.js

4. http://localhost:7000/git/users/Root-AR?sort=full_name. // TO get user (Root-AR) repo

5. http://localhost:7000/git/contributers/Root-AR/DS.  // TO get contributer for (ROOT-AR) for DS repo.

