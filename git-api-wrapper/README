Steps :

1. run npm install 
2. Go to GITHUB -> Create application and generate token manually or You can also use authorization API to generate token 
	While calling api please provide token in Http header :
	example : token : 'YOUR TOKEN'

3. run node server.js

4. http://localhost:7000/git/users/<OwnerName>?sortBy=full_name. // TO get user (Root-AR) repo

5. http://localhost:7000/git/contributers/<OwnerName>/<RepoName>.  // TO get contributer for (ROOT-AR) for DS repo.

